import LoginForm from "../components/forms/LoginForm";

function LoginPage() {
    return (
        <section>
            <h1>Log in</h1>
            <LoginForm />
        </section>
    );
}

export default LoginPage;